/*
package ApCSA;

public class WordScrambler {
    private String[] scrambledWords;

    public WordScrambler(String[] wordArr) {
        scrambledWords = mixedwords(wordArr);
    }

    private String recombine(String w1, String w2) {
        String halfW1 = w1.substring(0,w1.length()/2);
        String halfW2 = w2.substring(w1.length()/2);
        return (halfW1 + halfW2);
    }
}
*/
